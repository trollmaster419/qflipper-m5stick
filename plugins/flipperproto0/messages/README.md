To regenerate these headers, run in this directory:
```bash
nanopb_generator *.proto -D ../messages
```
You will need to install [nanopb](https://github.com/nanopb/nanopb) separately. If running from source, you can use `nanopb/generator/nanopb_generator.py`.
